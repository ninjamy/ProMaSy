<%= form_with(model: work_type, local: true) do |form| %>
  <% if work_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(work_type.errors.count, "error") %> prohibited this work_type from being saved:</h2>

      <ul>
      <% work_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :work_type %>
    <%= form.text_field :work_type, id: :work_type_work_type %>
  </div>

  <div class="field">
    <%= form.label :portfolio_id %>
    <%= form.number_field :portfolio_id, id: :work_type_portfolio_id %>
  </div>

  <div class="field">
    <%= form.label :edit_label %>
    <%= form.text_field :edit_label, id: :work_type_edit_label %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
