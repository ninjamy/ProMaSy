<p id="notice"><%= notice %></p>

<h1>Work Types</h1>

<table>
  <thead>
    <tr>
      <th>Work Type</th>
      <th>Portfolio</th>
      <th>Edit Label</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @work_types.each do |work_type| %>
      <tr>
        <td><%= work_type.work_type %></td>
        <td><%= work_type.portfolio_id %></td>
        <td><%= work_type.edit_label %></td>
        <td><%= link_to 'Show', work_type %></td>
        <td><%= link_to 'Edit', edit_work_type_path(work_type) %></td>
        <td><%= link_to 'Destroy', work_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Work Type', new_work_type_path %>
