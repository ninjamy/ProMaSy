<%= form_with(model: user_access, local: true) do |form| %>
  <% if user_access.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_access.errors.count, "error") %> prohibited this user_access from being saved:</h2>

      <ul>
      <% user_access.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :user_access_user_id %>
  </div>

  <div class="field">
    <%= form.label :portfolio_id %>
    <%= form.number_field :portfolio_id, id: :user_access_portfolio_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
