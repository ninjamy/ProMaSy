<%= form_with(model: user_access, local: true) do |form| %>
  <% if user_access.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_access.errors.count, "error") %> prohibited this user_access from being saved:</h2>

      <ul>
      <% user_access.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :UserID %>
    <%= form.number_field :UserID, id: :user_access_UserID %>
  </div>

  <div class="field">
    <%= form.label :PortID %>
    <%= form.number_field :PortID, id: :user_access_PortID %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
