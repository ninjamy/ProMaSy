<%= form_with(model: task_status, local: true) do |form| %>
  <% if task_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task_status.errors.count, "error") %> prohibited this task_status from being saved:</h2>

      <ul>
      <% task_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :task_status %>
    <%= form.text_field :task_status, id: :task_status_task_status %>
  </div>

  <div class="field">
    <%= form.label :portfolio_id %>
    <%= form.number_field :portfolio_id, id: :task_status_portfolio_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
